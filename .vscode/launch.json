{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Api",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "server.go",
            "cwd": "${workspaceFolder}"
        }
    ]
}